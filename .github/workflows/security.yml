name: Security Scan

on:
  # Run daily at 6 AM UTC
  schedule:
    - cron: '0 6 * * *'

  # Allow manual trigger
  workflow_dispatch:

env:
  BUN_VERSION: 'latest'

jobs:
  # ============================================================================
  # Daily Security Scan
  # ============================================================================
  trivy-scan:
    name: Trivy Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Trivy
        run: curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin

      - name: Run Trivy vulnerability scanner
        run: trivy fs --vuln-type os,library --severity CRITICAL,HIGH,MEDIUM,LOW --ignorefile .trivyignore --format sarif --output trivy-results.sarif .

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@9e907b5e64f6b83e7804b09294d44122997950d6 # v4
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Run Trivy (table output for logs)
        run: trivy fs --vuln-type os,library --severity CRITICAL,HIGH --ignorefile .trivyignore .

  # ============================================================================
  # Secret Detection
  # ============================================================================
  secret-scan:
    name: Secret Detection
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Trivy
        run: curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin

      - name: Run Trivy secret scanner
        run: trivy fs --scanners secret --format table .

      - name: TruffleHog Secret Scan
        uses: trufflesecurity/trufflehog@v3.88.0
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified

  # ============================================================================
  # Dependency Review (via Trivy)
  # ============================================================================
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Trivy
        run: curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin

      - name: Trivy dependency review
        run: trivy fs --vuln-type library --severity CRITICAL,HIGH,MEDIUM,LOW --ignorefile .trivyignore .

      - name: Trivy dependency review (summary)
        run: trivy fs --vuln-type library --severity CRITICAL,HIGH,MEDIUM,LOW --ignorefile .trivyignore --format json --output trivy-deps.json .

      - name: Generate summary
        run: |
          echo "## Dependency Review Results" >> $GITHUB_STEP_SUMMARY
          if [ -f trivy-deps.json ]; then
            TOTAL=$(jq '[.Results[]?.Vulnerabilities // [] | length] | add // 0' trivy-deps.json)
            echo "Total vulnerabilities found: **${TOTAL}**" >> $GITHUB_STEP_SUMMARY
          fi
        continue-on-error: true

  # ============================================================================
  # License Compliance
  # ============================================================================
  license-scan:
    name: License Compliance
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Trivy
        run: curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin

      - name: Run Trivy license scanner
        run: trivy fs --scanners license --severity UNKNOWN,HIGH,CRITICAL .
        continue-on-error: true

  # ============================================================================
  # Notify on Issues
  # ============================================================================
  notify:
    name: Notify
    runs-on: ubuntu-latest
    needs: [trivy-scan, secret-scan, dependency-review]
    if: failure()

    steps:
      - name: Create Issue on Failure
        uses: actions/github-script@v7
        with:
          script: |
            const title = `Security Scan Failed - ${new Date().toISOString().split('T')[0]}`;
            const body = `
            ## Security Scan Results

            The daily security scan has detected issues that require attention.

            **Workflow Run:** [View Details](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})

            ### Jobs Status
            - Trivy Scan: ${{ needs.trivy-scan.result }}
            - Secret Scan: ${{ needs.secret-scan.result }}
            - Dependency Review: ${{ needs.dependency-review.result }}

            Please review the workflow logs and address any security issues.
            `;

            // Check if issue already exists
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: 'security',
              state: 'open'
            });

            const existingIssue = issues.data.find(i => i.title.includes('Security Scan Failed'));

            if (!existingIssue) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: title,
                body: body,
                labels: ['security', 'automated']
              });
            }
